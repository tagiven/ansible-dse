{
    "cassandra": {
      "seed_hosts": "{% for seed in groups['dse-cluster'] -%}
          {{ hostvars[seed]['ansible_{{ cluster_interface }}']['ipv4']['address'] }}
          {%- if not loop.last %},{% endif -%}
          {%- endfor %}"
    }
}
